#include "bloom-filter.hpp"
#include <iostream>

int main(){
  // This is the main driver function. Need to decide if we handle CLI from here or in the other file. 
  // This is what will allow us to run our experiements.
}